// Copy this file to config.js and fill with your Firebase project keys
// You can find these in Firebase console > Project settings > General
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID"
  // Optional: measurementId, messagingSenderId, storageBucket, etc.
};
